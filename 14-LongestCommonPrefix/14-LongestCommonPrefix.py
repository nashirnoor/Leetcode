           temp = strs[0][i]
        for i in strs:
        ref_word = strs[0]
    def longestCommonPrefix(self, strs: List[str]) -> str:
            if len(i) < l:
                ref_word = i
                l = len(ref_word)
        l = len(ref_word)        
        for i in range(l):
        result = ""
           for j in strs:
             if j[i] != temp:
                return result
           result+=temp
        return result
            
        return ""
           
            
[
["flower","flow","flight"]
["dog","racecar","car"]
"fl"
""
"fl"
""
