class Solution(object):
    def commonFactors(self, a, b):
        """
        :type a: int
        :type b: int
        :rtype: int
        """
        count, n = 0, 1
        while n <= min(a,b):
            if a % n == 0 and b % n == 0:
                count += 1
            n += 1
        return count

        
1
